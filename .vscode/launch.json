{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "vice",
            "request": "launch",
            "name": "Balloon",
            "program": "${workspaceFolder}/build/balloon.prg",
        }
    ]
}